import styled from 'styled-components'

export const Container = styled.header`
  background: var(--alternative);
  height: 6rem;

  h1 {
    display: flex;
    align-items: center;
    padding: 1.5rem;
  }
`
